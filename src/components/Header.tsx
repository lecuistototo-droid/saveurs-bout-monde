import React from 'react';\nimport { useTranslation } from 'react-i18next';\nimport { useStore } from '@/store/useStore';\nimport { Moon, Sun, Globe } from 'lucide-react';\n\nexport const Header: React.FC = () => {\n  const { t, i18n } = useTranslation();\n  const { user, setTheme, setLanguage } = useStore();\n\n  const toggleTheme = () => {\n    setTheme(user.theme === 'light' ? 'dark' : 'light');\n    document.documentElement.classList.toggle('dark');\n  };\n\n  const changeLanguage = (lang: 'fr' | 'en' | 'es') => {\n    i18n.changeLanguage(lang);\n    setLanguage(lang);\n  };\n\n  return (\n    <header className=\"bg-cream dark:bg-dark-bg shadow-subtle sticky top-0 z-50\">\n      <div className=\"max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-4\">\n        <div className=\"flex items-center justify-between\">\n          <div className=\"flex items-center gap-2\">\n            <span className=\"text-3xl\">🌍</span>\n            <h1 className=\"text-2xl font-serif font-bold text-terracotta dark:text-terracotta-light\">\n              Saveurs du Monde\n            </h1>\n          </div>\n          \n          <nav className=\"hidden md:flex items-center gap-6\">\n            <a href=\"/\" className=\"text-gray-700 dark:text-gray-300 hover:text-terracotta transition\">\n              {t('common.home')}\n            </a>\n            <a href=\"/recettes\" className=\"text-gray-700 dark:text-gray-300 hover:text-terracotta transition\">\n              {t('common.recipes')}\n            </a>\n            <a href=\"/pays\" className=\"text-gray-700 dark:text-gray-300 hover:text-terracotta transition\">\n              {t('common.countries')}\n            </a>\n            <a href=\"/favoris\" className=\"text-gray-700 dark:text-gray-300 hover:text-terracotta transition\">\n              ❤️ {t('common.favorites')}\n            </a>\n          </nav>\n\n          <div className=\"flex items-center gap-4\">\n            <button\n              onClick={toggleTheme}\n              className=\"p-2 rounded-lg hover:bg-beige-dark dark:hover:bg-dark-card transition\"\n            >\n              {user.theme === 'light' ? <Moon size={20} /> : <Sun size={20} />}\n            </button>\n\n            <div className=\"flex items-center gap-2\">\n              <Globe size={18} className=\"text-gray-600 dark:text-gray-400\" />\n              <select\n                value={user.language}\n                onChange={(e) => changeLanguage(e.target.value as 'fr' | 'en' | 'es')}\n                className=\"bg-transparent border border-gray-300 dark:border-gray-600 rounded px-2 py-1 text-sm\"\n              >\n                <option value=\"fr\">Français</option>\n                <option value=\"en\">English</option>\n                <option value=\"es\">Español</option>\n              </select>\n            </div>\n          </div>\n        </div>\n      </div>\n    </header>\n  );\n};
