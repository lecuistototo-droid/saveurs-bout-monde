import React, { useState } from 'react';\nimport { useTranslation } from 'react-i18next';\nimport { useStore } from '@/store/useStore';\nimport { Search, X } from 'lucide-react';\n\nexport const SearchBar: React.FC = () => {\n  const { t } = useTranslation();\n  const { searchQuery, setSearchQuery } = useStore();\n  const [isFocused, setIsFocused] = useState(false);\n\n  return (\n    <div className=\"relative w-full max-w-2xl\">\n      <div className={`relative flex items-center bg-white dark:bg-dark-card rounded-2xl border-2 transition-all ${\n        isFocused ? 'border-terracotta shadow-elevated' : 'border-gray-200 dark:border-gray-700'\n      }`}>\n        <Search className=\"absolute left-4 text-gray-400\" size={20} />\n        <input\n          type=\"text\"\n          placeholder={t('common.search')}\n          value={searchQuery}\n          onChange={(e) => setSearchQuery(e.target.value)}\n          onFocus={() => setIsFocused(true)}\n          onBlur={() => setIsFocused(false)}\n          className=\"w-full pl-12 pr-4 py-3 bg-transparent outline-none text-gray-700 dark:text-white placeholder-gray-400\"\n        />\n        {searchQuery && (\n          <button\n            onClick={() => setSearchQuery('')}\n            className=\"pr-4 text-gray-400 hover:text-gray-600 transition\"\n          >\n            <X size={18} />\n          </button>\n        )}\n      </div>\n    </div>\n  );\n};
